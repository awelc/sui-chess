{
  "private": true,
  "description": "Root package for prettier-plugin-move (formats Move smart contracts). Frontend dependencies are in frontend/package.json.",
  "devDependencies": {
    "@mysten/prettier-plugin-move": "^0.3.5"
  }
}
